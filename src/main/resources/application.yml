spring:
  datasource:
    driver-class-name: org.postgresql.Driver

    # 🔥 Railway PostgreSQL 환경변수 기반 연결 (정확한 방식)
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}

    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 10000
      connection-timeout: 30000
      max-lifetime: 60000

  jpa:
    hibernate:
      ddl-auto: update   # 테이블 자동 생성
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never
      encoding: UTF-8

server:
  port: 8080
